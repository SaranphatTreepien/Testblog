import React from 'react';

function App() {
  const appStyles = {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    height: '100vh',
    backgroundColor: '#f0f0f0',
  };

  const textStyles = {
    fontSize: '3rem',
    color: '#333',
  };

  return (
    <div style={appStyles}>
      <h1 style={textStyles}>Hello World interested1</h1>
    </div>
  );
}

export default App;
